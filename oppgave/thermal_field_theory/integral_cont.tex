\begin{tikzpicture}
    \begin{axis}[axis lines=center, ticks=none, view/h=120, view/v=5]
    \addplot3+[%
        domain=0:4*pi,        
        samples=100,
        samples y=0, 
        no marks, 
        smooth, 
        ultra thick,
        blue
    ]
    ({2*cos(deg(x))}, {0},  {2*sin(deg(x))});
    %
    \addplot3[%
        opacity = 0.1,
        mesh,
        black,
        z buffer = sort,
        samples = 50,
        variable = \u,
        variable y = \v,
        domain = 0:180,
        y domain = 0:360,
    ]
    ({2*cos(u)*sin(v)}, {2*sin(u)*sin(v)}, {2*cos(v)});
    \end{axis}
\end{tikzpicture}
  